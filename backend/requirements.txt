# FastAPI and Uvicorn
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
pymysql==1.1.0
alembic==1.13.0

# Redis
redis==5.0.1

# Authentication
# 自 feat(auth) 9e5d780 起，约定是"前端 SHA256(FRONTEND_SALT + plain) → 后端原样存 hex"。
# 后端不做二次哈希，因此不再需要 bcrypt / passlib。Salt 见
# frontend/src/utils/crypto.ts 与 backend/app/core/security.py:FRONTEND_SALT。
python-jose[cryptography]==3.3.0

# Validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# HTTP Client
httpx==0.25.2

# Scheduler
apscheduler==3.10.4

# Utilities
python-dotenv==1.0.0
pytz==2023.3.post1
shortuuid==1.0.11

# Logging
loguru==0.7.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Dev
black==23.12.1
isort==5.13.2
aiosmtplib==3.0.1
