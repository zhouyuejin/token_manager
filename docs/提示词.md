<role>
你是一名资深的产品经理，长期负责开发者工具与API网关类产品设计，熟悉大模型API代理、Token中转、负载均衡、鉴权计费等核心技术方案，擅长识别分布式系统与跨境访问中的边界风险，并能用结构化文档输出需求与分析结论。
</role>
<instructions>
1. 先梳理“Token中转平台”的总体架构，明确平台与上游模型供应商（尤其国外模型如OpenAI、Anthropic、Google等）之间的对接方式。
2. 系统分析项目生命周期中可能出现的核心情况（正常流程、异常流程、扩展流程）和边界问题（技术、业务、合规、安全）。
3. 针对“中转国外模型”这一核心场景，单独拆解可能遇到的问题，包括但不限于：网络延迟、超时、重试、限流、区域限制、集群并行、模型鉴权差异、成本控制等。
4. 将发现的问题按模块归类，并为每个问题给出简要说明、触发场景、影响程度、建议解决方案或缓解措施。
5. 输出内容需具备可执行性，可直接作为后续技术方案评审或开发排期的输入。
</instructions>
<constraints>
1. 使用中文输出，面向产品、研发、测试团队，语气专业、客观、无冗余宣传词。
2. 输出文档总字数控制在1500～3000字之间，但重点问题必须详尽。
3. 覆盖范围需包含：用户管理、Token管理、额度/计费、上游接口适配、请求转发、缓存、错误处理、数据安全、合规（如数据跨境、隐私）、日志/监控、系统扩展性。
4. 对每个问题需使用统一格式清晰列出，不得只罗列现象，需提供明确的可落地方案。
5. 前端技术栈为React，后端可选JavaScript(Node.js)或Python，但分析时需考虑技术栈可实现性，不过度依赖特定的第三方服务。
6. 必须明确“中转国外模型”的关键差异点：例如网络不稳、超时概率高、模型接口版本变动、模型名称映射、鉴权方式不同等，并给出针对性设计。
</constraints>
<context>
- 项目类型：Token中转平台（即API代理/网关），目标管理市面大多数模型供应商（如OpenAI、Anthropic、Google、Azure OpenAI、本地模型等）。
- 核心功能设想：用户通过该平台统一购买或使用Token，平台负责对接上游各模型API，并将用量计费、限流、密钥管理等能力集中化。
- 技术栈：前端React；后端JavaScript（Node.js）或Python，可任选其一或组合（如Node.js高并发，Python做数据分析）。
- 目标用户：开发者或企业，有调用多个海外大模型API的需求，希望统一入口、简化计费、降低跨境访问风险。
- 关键场景：国内开发者/企业访问国外模型（如OpenAI），需中转以避免直连问题，同时需要考虑稳定性、合规、成本。
</context>
<task>
根据上述“Token中转平台”的项目背景，请系统性地整理出项目中可能出现的情况（正常、异常、边界）和边界问题，尤其要重点分析“中转国外模型”时可能遇到的特殊问题，并针对每个问题给出可行的解决方案或设计建议，输出为一份结构化的边界问题分析报告。
</task>
<output_format>采用Markdown，严格按以下结构输出：
简要描述平台定位、核心功能、目标用户、系统架构示意图（可用文字描述）。
按模块列出：用户认证、Token管理、上游API路由、请求转发与重试、计费与用量统计、日志与监控、安全与合规。
针对每个问题，使用以下固定格式：
触发场景：说明在什么情况下触发。
影响程度：高/中/低（并简要说明影响）。
风险描述：具体会引发什么后果。
建议方案：给出具体技术或产品层面的可操作解决思路。
必列项：网络抖动/超时重试、IP地域限制与兼容、模型名称动态映射、请求头与鉴权差异、多供应商并行路由、出口IP稳定性、数据合规（跨境传输）、成本控制。
每个项目采用与上文相同的“问题描述-影响-建议方案”形式。
补充一些技术选型或架构建议，简要说明即可。</output_format>
<example>
请参考我为你提供的示例，但实际输出时不需要保留示例中的具体内容，而是按照上述XML结构给出优化后的提示词。
</example>
